Rails.env.on(:any) do
  config.assets.configure do |env|
    env.register_transformer  "application/ecmascript-6",
                              "application/javascript",
                              Sprockets::BabelProcessor.new(
                                "modules"    => "amd",
                                "moduleIds"  => true
                              )
  end
end
